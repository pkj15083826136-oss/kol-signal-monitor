# KOL Signal Monitor Scheduler

This repository runs the four-chain KOL/smart-money monitor every minute through GitHub Actions.

The dashboard is deployed separately. Required Actions secrets:

- `SITE_URL`
- `MONITOR_SECRET`

Signal thresholds: 6 / 18 / 38 / 58 monitored wallets.
